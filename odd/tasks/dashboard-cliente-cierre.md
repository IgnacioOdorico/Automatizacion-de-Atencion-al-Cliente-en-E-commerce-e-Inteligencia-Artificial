# ODD — Dashboard cliente: cierre (backend fixes, E2E, Fase 7, Fase 8, push)

**Rama:** `feature/dashboard-cliente` · **Spec fuente:** `SPEC_DASHBOARD_CLIENTE.md` §6/§8/§9
**Autorización del usuario (2026-09-20):** resolver todas las decisiones pendientes; `connections.py` y todo lo de la rama se puede modificar (nada de esto está en `main`); al terminar, push a una rama que NO sea `main`. No se toca lo que está en `main` (Flujos 1 y 2, schema existente).
**TDD:** activo · **Runners:** `npm test` + `npm run build` (dashboard-web), `pytest` (dashboard-api, venv en `dashboard-api/.venv`)
**Estrategia de entrega:** `ask-on-risk`; push de la rama de feature (sin PR salvo pedido).

## Decisiones tomadas (antes eran "necesitan decisión")
1. Ruta de callback Gmail: el backend redirige directo a `/conexiones` (fuente del error); el alias `/connections` del front queda como red de seguridad.
2. Errores del callback de Gmail: el backend redirige a `/conexiones?gmail=error&reason=<código>` en vez de devolver JSON crudo.
3. Cancelar código Telegram: endpoint nuevo `DELETE /connections/telegram/code` que invalida el código pendiente; el front lo usa al cancelar.
4. Untracked: se versionan `openspec/`, `docs/DESVIOS_SPEC.md`, `SPEC_DASHBOARD_CLIENTE.md`; se ignoran `.atl/` y `.opencode/` (tooling de agentes).

## Límites honestos
Credenciales de Google (`DASHBOARD_GOOGLE_CLIENT_ID/SECRET`) y bot de Telegram (`DASHBOARD_BOT_TOKEN_VINCULO`) están vacías en `.env` y son del usuario: no se pueden crear ni cargar. Se verifica todo lo demás; Telegram se prueba simulando el webhook de n8n con curl (criterio de la spec 4.3) y Gmail hasta el 503 controlado.

## Tareas
- [x] C1 Backend: callback Gmail redirige a `/conexiones` y ante errores a `?gmail=error&reason=...`; front mapea `reason` a mensaje (TDD pytest + vitest)
- [x] C2 Backend+front: `DELETE /connections/telegram/code` + cancelar en la card (TDD)
- [x] C3 Housekeeping: `.gitignore` (`.atl/`, `.opencode/`) y commit de `openspec/`, `docs/DESVIOS_SPEC.md`, `SPEC_DASHBOARD_CLIENTE.md` (commits `3e85fe2` y `2a5ca3c`)
- [x] C4 Docker: levantar stack completo, aplicar migración/seed, smoke de API, aislamiento entre cuentas, Telegram confirm por curl, WhatsApp pending, `orden-nueva` → dashboard en vivo; corregir lo que rompa (**parcial: el punto 9 — Flujo 1 real de n8n → email en Mailpit — queda `blocked` por credenciales de n8n, ver evidencia**)
- [x] C5 Fase 7 (pulido para cámara): estados carga/vacío/error, datos en pantalla, transiciones, demo en vivo (**parcial: el disparo real de `demo_en_vivo.ps1` queda `blocked` por credenciales de n8n y la revisión visual con la sesión real del usuario queda pendiente; ver evidencia**)
- [x] C6 Fase 8 (QA seguridad §6): checklist 9.1–9.8 con evidencia, corregir huecos (**parcial: 9.1 y 9.8 quedan sin tildar por salvedades; el circuito real de Gmail/Telegram y el Flujo 1 real quedan sin verificar por credenciales; ver evidencia y "Pendientes del usuario"**)
- [ ] C7 Push de la rama a GitHub (no `main`) y reporte

## Ruta por tarea
C1, C2, C5, C6: delegated direct (un writer por vez). C3, C7: inline (mecánico). C4: delegated direct.

## Progreso / Evidencia
- **C1** (commit `1fe96a7`) — Backend (`dashboard-api/app/routers/connections.py`): el callback valida primero el `state` firmado y responde SIEMPRE con redirect 307 (el que ya usaba el archivo) a `{DASHBOARD_FRONTEND_URL sin barra final}/conexiones?...`; éxito `?gmail=connected`, fallo `?gmail=error&reason=<código>` con catálogo cerrado `GmailErrorReason`: `denied` (access_denied), `google_error` (otro `error` de Google), `invalid_state` (ausente/firma mala/vencido/otro canal), `missing_code`, `exchange_failed` (Google rechaza el canje 4xx o no trae access_token), `upstream` (red/timeout/5xx/JSON inválido), `no_email`, `no_refresh`, `internal` (falla al guardar en BD; se loguea solo el nombre de la excepción). Nunca viajan detalles, tokens ni `error_description`; el destino sale de `settings`, no del request. Sin persistencia en ningún fallo. Front (`lib/connections.ts`): `parseGmailReturn` devuelve `{result, reason}` con `reason` validado contra el catálogo (`Object.hasOwn`); `gmailReturnNotice` mapea cada motivo a un mensaje en voseo y cualquier valor fuera del catálogo cae al genérico sin reflejarse. Alias `/connections` del front intacto (red de seguridad).
  - Backend RED: `pytest tests/test_gmail_oauth.py` 20 fallos / 5 OK -> GREEN 25/25. `pytest` completo: 105 passed (baseline previo 91).
  - Front RED: `vitest run src/__tests__/connections.test.ts` 10 fallos / 38 OK -> GREEN 48/48. `npm test`: 7 archivos, 100 tests OK. `npm run build`: OK.
  - No verificado: consentimiento real de Google (credenciales vacías en `.env`); el redirect se prueba con `TestClient` y Google mockeado con `httpx.MockTransport`.
- **C2** — Backend: `telegram_codes.cancel(account_id)` (bajo el lock; solo toca el código indexado por esa cuenta, idempotente, devuelve True solo si había un código vigente) y `DELETE /connections/telegram/code` (JWT; 200 `{"cancelled": bool}`). No colisiona con `DELETE /connections/{channel}`: ese path es de un solo segmento, y hay tests que verifican que cancelar el código NO desconecta el canal y que `DELETE /connections/telegram` sigue funcionando. Aislamiento entre cuentas verificado por API con una segunda cuenta registrada. Front: `connectionsApi.telegramCancelCode()`, `shouldCancelTelegramCode` (solo un código vigente pega al server; uno vencido se descarta local) y `useTelegramLink.cancel`: si el DELETE falla, el código local NO se descarta (sigue vigente en el server), se muestra el error (`telegram-cancel`) y el botón permite reintentar; `dismiss` queda como descarte local (lo usa la desconexión del canal).
  - Backend RED: `pytest tests/test_telegram_codes.py tests/test_telegram_connection.py` 11 fallos / 17 OK -> GREEN 28/28. `pytest` completo: 118 passed.
  - Front RED: `npm test` 7 fallos / 102 OK -> GREEN 109/109 (8 archivos). `npm run build`: OK.
  - No verificado: el comportamiento del hook/card en navegador (no hay React Testing Library en el repo; la lógica de decisión está en funciones puras testeadas y el cableado lo valida `tsc`).

- **C4** — E2E con Docker (`docker compose up -d --build`; 6 servicios up/healthy; `dashboard-api` resuelve `postgres` por nombre y consulta la BD). Guardas de datos: `pg_dump` completo previo (fuera del repo) y conteos antes/después idénticos (`products 8, orders 22 [measured], order_items 1, stock_alerts 0, interactions 0, tickets 5 [measured], faq_responses 6`, stock intacto, cuenta demo con sus 3 conexiones). Todo lo creado llevó prefijo `ORD-E2E-`/`e2e-…@example.com` y se borró acotado por prefijo.
  - Migración y seed ya estaban aplicados (idempotentes). `orders` es `TIMESTAMPTZ` en esta instancia; la API serializa ISO 8601 con `Z`. `/dashboard/summary` coincide con `v_metrics_summary` (22/6/90.00/120.00/0/5/2); `?data_source=measured` filtra bien.
  - Smoke OK: `/health`, register/login/`/me`, `/orders` (paginado, `?status=`, detalle con `order_items` y `raw_payload`, 404), `/tickets` (+detalle), `/products` (`?search=`, paginado), `/connections` (demo: 3 canales; cuenta nueva: 3 `disconnected`). Sin JWT / JWT inválido / JWT forjado / `alg=none` / refresh usado como access → 401 en todos los endpoints autenticados. Aislamiento: la e2e pidió WhatsApp y vinculó Telegram sin alterar la demo; ids de conexión disjuntos. Telegram: start → 6 dígitos (900s); confirm sin header / secreto malo / vacío → 401; código inexistente, ya usado o cancelado → 400; confirm OK → `connected` con `external_reference = chat_id`; `DELETE /connections/telegram/code` idempotente y sin desconectar el canal; `DELETE /connections/telegram` deja `disconnected` con referencia, credenciales y `connected_at` en NULL. WhatsApp: E.164 válido → `pending` (verificado en BD); inválido → 422; la demo sigue `connected`. Gmail: `oauth-url` → 503 controlado; callback con `state` inválido → 307 a `…/conexiones?gmail=error&reason=invalid_state` (directo y vía `/api`).
  - Front (`:8080`): `/` y rutas profundas (`/conexiones`, `/pedidos/123`) sirven `index.html`; assets con MIME correcto y 404 real si faltan; `/api/*` llega al backend (login por el proxy). CORS: preflight desde `:5173` y `:8080` permitido; origen ajeno (`evil`, `null`) sin `Access-Control-Allow-Origin` (400); nunca `*`.
  - **Bugs reales corregidos (TDD)**: (1) nginx daba 502 en `/api` si `dashboard-api` cambiaba de IP (rebuild/reinicio de Docker) porque resolvía el upstream una sola vez; reproducido (IP `.2`→`.9` ⇒ 502) y arreglado con `resolver 127.0.0.11` + `proxy_pass` con variable + `rewrite` (mismo escenario ahora 200); test de guarda `nginxConf.test.ts` RED 4 fallos/1 OK → GREEN 5/5. (2) Spec connections: al registrar debían crearse las 3 filas `disconnected`; `/me` de una cuenta nueva devolvía `connections: []` hasta pedir `/connections`; ahora el alta las crea en la misma sentencia (CTE atómico); RED → GREEN. (3) `/products?search=` trataba `%` y `_` como comodines de `ILIKE` (`%` devolvía todo el catálogo); ahora se escapan (RED → GREEN). `pytest`: 120 passed (baseline 118). `npm test`: 9 archivos, 114 OK. `npm run build`: OK.
  - **BLOCKED — punto 9 (Flujo 1 en vivo)**: la instancia de n8n no tiene ningún workflow ni credencial (`workflow_entity` y `credentials_entity` vacías). Se importó `Flujo 1 — Pipeline de Procesamiento de Órdenes.json` (id `797bI0eXTmiaSmvJ`) con el CLI y se activó (`update:workflow --active=true` + reinicio de n8n; arranca `Activated workflow`). Al disparar `POST /webhook/orden-nueva` con `ORD-E2E-001`, n8n responde 200 sin cuerpo y la ejecución falla: `Credential with ID "3VBjtgn4ueU5ZLRx" does not exist for type "postgres"`; no se escribió nada (0 órdenes). Falta, y es del usuario crearlo en la UI de n8n: credencial **Postgres** (`postgres:5432`, BD `ecommerce_tesis`, usuario `n8n_user`) y credencial **SMTP** (`mailpit:1025`, sin auth, sin TLS) y asignarlas a los nodos del workflow (los ids del JSON, `3VBjtgn4ueU5ZLRx` y `05ihlktQezmjllAa`, no existen en esta instancia; la ejecución fallida `id 1` quedó en el historial de n8n). El workflow queda importado y activo. No verificado: escritura en `orders`, descuento de stock, email en Mailpit (0 mensajes) y la rama `no_stock` por n8n.
  - Lado dashboard del "en vivo" verificado con **simulación por SQL** (NO es n8n): se insertaron `ORD-E2E-001` (confirmed) y `ORD-E2E-002` (no_stock, cantidad 99) con `data_source='synthetic'`; `GET /orders` las lista primero, `?status=no_stock` incluye la nueva, `pedidos hoy` pasó de 0 a 2 y los promedios MTTD/MTTR de `summary` coincidieron con `v_metrics_summary` (76.38/101.92 con las 2 filas). Borradas al terminar.
  - Observaciones sin cambiar: `DASHBOARD_FRONTEND_URL` por defecto es `http://localhost:5173` (dev Vite); con el front en Docker (`:8080`) el callback de Gmail redirige a `:5173`, así que hay que setearla en `.env` si se demuestra desde `:8080`. `orders`/`tickets`/`products` son globales (no hay `client_account_id` en esas tablas); el aislamiento por cuenta aplica a conexiones y perfil.

- **C5** (commits `1c94219`, `d7d2bdf`, `6255a56`, `5da3ec4`, `4e49118`, `213803d`) — Pulido de `dashboard-web/` + script de demo. Backend, workflows, `init_simple.sql` y BD sin cambios (0 escrituras: solo lecturas SQL para ver los datos reales).
  - **8.1** `QueryView` (contrato cargando/error/vacío/listo) + `ErrorState` (Reintentar) + `Skeleton`/`TableSkeleton` + `LiveBadge`, usados por Dashboard, Pedidos, Tickets, Catálogo, Conexiones, Perfil y el modal de pedido. Red caída ≠ 401 ≠ 5xx (`lib/viewState.ts`); un refetch fallido con datos previos mantiene la pantalla y muestra una franja "Mostramos los últimos datos que tenemos". Login/Registro ya distinguían red/401 (`friendlyApiError`; ahora sin jerga de desarrollo ni el status crudo del proxy).
    - RED: `viewState.test.ts` (módulo inexistente) y `messages.test.ts` 2 fallos/2 OK -> GREEN 16 + 4. Render en jsdom (`queryView.test.tsx`, 6 OK: esqueleto, datos, vacío, error de red con Reintentar que llama a `refetch`, 401 distinto, refetch fallido con datos). `npm test`: 134 OK.
  - **8.2** Fechas `dd/mm/aaaa hh:mm` en hora de Mendoza (antes `toLocaleDateString` mostraba `20/09/2026, 02:32 p. m.` y dependía de la máquina), montos negativos `-$5,00`, `formatContact` (teléfono/chat/correo del ticket), `dataSourceLabel`, `formatMetricDuration` (TMR sin interacciones = `—` en vez de `0s`), detalle de pedido con campos legibles + JSON plegado (`lib/orderDetail.ts`), ítems con nombre de producto, truncado con tooltip, tablas en fichas etiqueta/valor en móvil, sidebar -> barra de íconos en móvil, jerga interna fuera del texto visible (pipeline/webhook/Flujo 1/`Fuente: Medido (flujos)`), `PlaceholderPage` muerto eliminado.
    - RED: `format.test.ts` 11 fallos (+ `orderDetail.test.ts` sin módulo) -> GREEN 34/34; también pasa con `TZ=UTC` y `TZ=Asia/Tokyo` (24/24 en `format.test.ts`).
  - **8.3** `Modal` común (Esc cierra, Tab atrapado, foco entra y vuelve al disparador, sin scroll de fondo; 10 tests jsdom), número de pedido como botón (antes la fila solo respondía al mouse), fila nueva del polling resaltada ~3 s (`useFreshRows`), hover en cards, badge con transición, entrada de pantalla/modal, `prefers-reduced-motion` (spinner sigue girando lento), enlace "saltar al contenido", `--focus-ring` sólido. Contraste AA verificado contra el CSS real (`contrast.test.ts`, 23 casos): RED 6 fallos/17 OK (`--text-3` daba 3.5-4.05:1 y el botón primario 3.8:1) -> GREEN con `--text-3: #7c8699` y degradé del botón más profundo (misma identidad índigo). `a11yStyles.test.ts` RED 8 fallos -> GREEN (anillo de foco >= 3:1, bloque `prefers-reduced-motion`).
  - **8.4** `demo_en_vivo.ps1` (raíz; UTF-8 con BOM, estilo `backup.ps1`): `ORD-DEMO-<yyyyMMddHHmmss>`, `-Sku` (default `PROD-001`), `-Cantidad`, `-SinStock` (9999 u.), `-ConChatbot`/`-Mensaje` (payload plano `{from, text:{body}, name}` a `/webhook/whatsapp-business`), valida `/healthz`, distingue 404 (flujo inactivo) de 200 sin cuerpo (ejecución fallida), advertencia de escritura en la BD compartida en la cabecera y confirmación (`-Si` para saltarla). Documentado en `dashboard-web/README.md`. Verificado: parser de PowerShell 5.1 sin errores (`[Parser]::ParseFile`) y `GET /healthz` de n8n = `{"status":"ok"}`. **NO se ejecutó** (n8n sin credenciales cargadas: el disparo real sigue `blocked`).
  - **Revisión en navegador (parcial)**: sin loguearse ni crear cuentas; se sirvió el build con `vite preview` contra una API de mentira local (fixtures con los datos reales de la BD, token falso) y se revisó en 1280 px y 375 px: sin scroll horizontal de página en las 6 pantallas móviles, Pedidos entra en 935 px, fila nueva resaltada al llegar por polling (aparece a los ~4 s y se apaga a los ~3.2 s), franja de error + Reintentar, pantalla de error con Reintentar que recupera, Esc cierra el modal y devuelve el foco. Ese ajuste dejó `213803d`. Servidores de prueba detenidos. No verificado: la sesión real del usuario ni Inter cargada desde Google Fonts (sin red, cae a la fuente del sistema).
  - Datos que se ven raros **por los datos** (no se tocaron): 11 de 22 órdenes sin `processed_at`/`notified_at` (Pedidos muestra solo "Recibido" en esas); nombres/asuntos sin acentos (`Lucia Fernandez`, "Cambio de direccion…"); un cliente repetido en varias órdenes (Jorge Molina x3, etc.) y `Slow Topas Tipografia` como cliente; `interactions` vacía (TMR `—`, Interacciones 0); ticket 4 `resolved` sin `resolved_at` ("Sin fecha registrada"); una sola orden con `raw_payload` (`ORD-2026-0110`); Categoría vacía si el producto no tiene.
  - Final: `npm test` **220 OK (18 archivos; baseline 114/9)**, `npm run build` OK.

- **C6** (commits `325b6bb`, `28a153c`, `99600e6`, `10acad6`, `97afbe4`, `3fd137b`, `47cc98c`, `b057a66`, `ea2f25f` + docs `ef0f9b0`, `567b0d6`) — QA de seguridad. Nunca se leyó ni imprimió `.env`: el permiso lo deniega y no se esquivó. Sin valores de secretos en este documento.
  - **Huecos corregidos (cada uno con test que falló antes)**: (a) `DELETE /connections/telegram` ahora invalida el código pendiente de esa cuenta (RED 1 fallo -> GREEN, `325b6bb`). (b) `DASHBOARD_FRONTEND_URL` por defecto `http://localhost:8080` en el compose + guarda de coherencia con `CORS_ORIGINS` (`28a153c`). (c) nginx: `nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy` y CSP estricta (`script-src 'self'`, sin `unsafe-inline`/`unsafe-eval`, `connect-src 'self'`, `frame-ancestors 'none'`, Google Fonts permitido), `server_tokens off`, tope de body 1 MB y `X-Forwarded-For` pisado en vez de concatenado; `security-headers.conf` se repite en `/assets/` porque nginx descarta los headers del server si un `location` define uno propio (`99600e6`, RED por archivo inexistente -> 16/16 en `nginxConf.test.ts`). (9.2) **fail-closed**: la API no arranca con `DASHBOARD_JWT_SECRET`/`DASHBOARD_N8N_SECRET`/`DASHBOARD_ENC_KEY` vacíos, < 32 caracteres, de ejemplo (`demo-…-cambiar`, que estaban como default en el compose y en `config.py`), iguales entre sí o (la clave) que no sea Fernet válida; el error nombra variables, nunca valores; el compose ya no trae defaults y `DATABASE_URL` usa `POSTGRES_PASSWORD` (RED 29 fallos -> GREEN, `10acad6`). (9.5) IP real del cliente detrás de nginx (`core/client_ip.py`, `DASHBOARD_TRUSTED_PROXIES` = servicio `dashboard-web`, solo `X-Real-IP`, valida que sea IP): antes todos los clientes compartían el bucket del proxy (DoS del login) (`97afbe4`). (e) Hardening de bajo riesgo (`3fd137b`, RED 15 fallos -> GREEN): login sin oráculo de timing (siempre corre un bcrypt), JWT exige `exp`/`sub`/`type` y `sub` numérico, registro de refresh jti usados atómico y sin crecer para siempre, `compare_digest` sobre bytes (un `X-N8N-SECRET` no ASCII daba 500), topes de largo (login, registro con límite de 72 bytes de bcrypt, `code`/`chat_id`), `/docs`/`/redoc`/`/openapi.json` apagados (`DASHBOARD_ENABLE_DOCS`), CORS con métodos y headers explícitos, rate limit sin buckets vacíos, Dockerfile de la API sin root (`uid 10001`). `DASHBOARD_ALLOW_REGISTRATION` (default `true`) para cerrar el alta en instalaciones públicas (`ea2f25f`).
  - **9.1**: `SELECT` en BD: `password_hash` empieza con `$2b$12$` y mide 60; `seed_dashboard.sql` guarda solo el hash; ningún `password = '…'` plano en los `.sql`. **Salvedad**: `Demo2026!` (cuenta demo, pública a propósito) figura en claro en `dashboard-web/README.md`, `tests/conftest.py`, `tests/test_security.py` y `validate.test.ts`. Veredicto: cumple con salvedad (sin tildar).
  - **9.2**: token de login `expires_in` 7200; `exp-iat` del access 7200 s y del refresh 604800 s; refresh usado como access en `/me` y `/orders` -> 401; `alg=none` -> 401; firma alterada -> 401; sin token / `Token abc` -> 401; los 6 endpoints autenticados sin JWT -> 401; refresh #1 -> 200 con tokens nuevos, reutilizar el refresh rotado -> 401, el nuevo funciona; access en `/auth/refresh` -> 401. En Docker: `docker compose run … -e DASHBOARD_JWT_SECRET=` (y ENC_KEY, N8N_SECRET vacíos, y el secreto de ejemplo) -> `InsecureConfigurationError` nombrando la variable; con el `.env` del usuario el contenedor arranca `healthy` (o sea, sus valores pasan la validación). Cumple.
  - **9.3**: `test_persisted_credentials_are_fernet_ciphertext_never_the_token`: el `cc::text` de la fila entera no contiene el refresh token, `encrypted_credentials` empieza con `gAAAA` y descifra al JSON esperado; dos cifrados del mismo texto difieren; un token adulterado lanza `InvalidToken`; `/me` y `/connections` nunca devuelven el campo ni el criptograma. Verificado contra la BD de pruebas con Google simulado (`httpx.MockTransport`). Cumple.
  - **9.4**: preflight con curl a `/auth/login`: `:5173` y `:8080` -> 200 con su `Access-Control-Allow-Origin`; `evil.example.com`, `null` y `localhost:8080.evil.com` -> 400 sin header; ningún `Access-Control-Allow-*` con `*` (test). Cumple.
  - **9.5**: contra `:8000`: 5 x 401 y el sexto **429**; con la clave correcta y `X-Real-IP` spoofeado sigue 429. Vía nginx (`:8080/api`) rotando `X-Real-IP` y `X-Forwarded-For` con emails distintos: 5 x 401 y el sexto **429** (nginx pisa el header). Desde otro contenedor de la red (otra IP real) contra el mismo nginx: 401 (bucket propio) mientras el host seguía en 429. Cumple.
  - **9.6**: sin header, header vacío, secreto malo y secreto no ASCII -> 401 (directo y vía nginx); flujo completo con el secreto correcto cubierto por `test_telegram_connection.py` (28 tests) y C4. Cumple.
  - **9.7**: `git grep` sobre HEAD (patrones `sk-`, `AIza`, `GOCSPX-`, bot token `\d{8,10}:…`, JWT, clave privada, tokens de GitHub/Slack/AWS, blobs CryptoJS de n8n): 0 coincidencias. Historial: script que recorre `git log -p` de **todas las refs (105 commits)** y de `main..HEAD` (34 al momento de auditar) buscando esos patrones más claves Fernet de 44 caracteres y asignaciones `*SECRET|TOKEN|PASSWORD|KEY = valor`: sin secretos reales; solo falsos positivos (nombres de variables en código, fixtures de test y el hash bcrypt de la cuenta demo). `git ls-files .env` vacío, `.gitignore:7` lo ignora y nunca fue añadido en ninguna commit; `.env.example` y `dashboard-api/.env.example` con valores vacíos o placeholders; el compose ya no tiene defaults de secretos. **Nada que rotar.** Cumple. Observación: defaults débiles de la infra previa (`n8n_pass`, `admin123`, `admin`) siguen en el compose; no son secretos reales pero conviene pisarlos en `.env`.
  - **9.8**: revisadas todas las queries de los routers autenticados: solo `/me` y `/connections*` filtran por `client_account_id` (del token, nunca del request: `?client_account_id=` se ignora); `orders`/`tickets`/`products`/`dashboard` son globales (sin la columna, no modificables). No hay endpoint que reciba un id de conexión o de cuenta por path, así que no hay ids ajenos que filtrar; los ids de recursos globales inexistentes dan 404 igual para todas las cuentas. Con dos cuentas (curl y `test_isolation.py`, 13 tests, con verificación por mutación quitando el filtro): conexiones, WhatsApp pendiente, códigos de Telegram y estado de Gmail no cruzan cuentas, y una desconexión de B no invalida el código de A. **Salvedad**: los datos globales sí se ven entre cuentas (documentado en SPEC §10.2). Veredicto: cumple con salvedad (sin tildar).
  - **d) tokens en `localStorage`**: se dejó el esquema (sin reescribir auth); mitigado por la CSP y por `securityGuards.test.ts` (ningún `console.*`, solo `tokenStorage` toca `localStorage`, sin `innerHTML`/`eval`, token solo en el header `Authorization` o el body, nunca en una URL). Documentado en SPEC §10.5.
  - **CSP en navegador real**: con el contenedor web reconstruido, login por la UI con una cuenta e2e y recorrido de las 6 pantallas sin ninguna violación de CSP (`securitypolicyviolation`), estilos y datos cargados; una inyección de `<script>` inline y una imagen externa sí se bloquearon. `nginx -t` OK; los headers salen en `/`, `/assets/*`, `/api/*` y en los 404.
  - **Regresión final** (`docker compose up -d --build`, 6 servicios up; API y Postgres `healthy`): `pytest` **216 passed** (baseline 120); `npm test` **238 passed** en 19 archivos (baseline 220/18); `npm run build` OK. Smoke autenticado con cuentas e2e contra `:8000` y `:8080/api`: `/health`, register/login, `/me`, `/dashboard/summary`, `/connections` (3 canales) y la SPA `/`, todos 200. Cuentas `e2e-c6-…@example.com` borradas con `DELETE` acotado por ese patrón; conteos idénticos a los del inicio (`products 8, orders 22, order_items 1, tickets 5, interactions 0, faq_responses 6, client_accounts 1, channel_connections 3`) y la cuenta demo intacta (whatsapp y telegram `connected`, email `disconnected`). Stack dejado levantado.
  - **No verificado / bloqueado**: Gmail y Telegram reales (credenciales de Google, bot de vínculo y URL pública son del usuario); el Flujo 1 real de n8n (sin credenciales cargadas); el circuito real del secreto de n8n de punta a punta (solo simulado por curl/tests); Google Fonts desde el navegador (sin red externa); el segundo `.env.example` no se pudo editar (permiso denegado; ver pendientes).
  - **Recomendaciones (no implementadas)**: (1) cerrar el registro (`DASHBOARD_ALLOW_REGISTRATION=false`) o agregar `client_account_id` a las tablas globales antes de exponer la instalación a internet (hoy cualquier cuenta lee los datos de clientes finales). (2) El estado de refresh rotados, rate limit y códigos de Telegram es en memoria: un reinicio permite reutilizar un refresh ya rotado hasta que venza y borra los códigos; con más de una réplica o en producción, persistirlo (BD o Redis). (3) `get_current_account_id` no verifica que la cuenta siga existiendo (solo `/me` lo hace): un token de una cuenta borrada sirve hasta 2 h. (4) El bloqueo por email permite a un atacante bloquear el login de una víctima 5 min (5 fallos); es el trade-off del límite por email. (5) Producción: HTTPS + HSTS, `Secure` en la cookie de refresh, access en memoria y refresh solo en cookie HttpOnly. (6) Los puertos de PostgreSQL, n8n, Grafana y Mailpit se publican en todas las interfaces con defaults débiles (`n8n_pass`, `admin123`, `admin`): pisarlos en `.env` y publicar en `127.0.0.1`. (7) El access log de uvicorn registra el query string del callback de Gmail (`state` y `code`): filtrarlo. (8) El scope `gmail.modify` es más amplio de lo que usa la app; reducirlo cuando se defina qué hace el canal. (9) `DASHBOARD_DATABASE_URL` en el `.env.example` raíz no la lee nadie (la API usa `DATABASE_URL`, fija en el compose). (10) Registro con respuesta 409 permite enumerar emails registrados (inherente al registro abierto).

## Pendientes del usuario

1. **Credenciales de Google** para conectar Gmail: cliente OAuth (Web) en modo testing con `DASHBOARD_GOOGLE_CLIENT_ID`, `DASHBOARD_GOOGLE_CLIENT_SECRET` y `DASHBOARD_GOOGLE_REDIRECT_URI` (`http://localhost:8000/connections/gmail/callback`, idéntica a la URI autorizada en Google Cloud Console).
2. **Bot de vínculo de Telegram**: crear el bot con @BotFather (distinto del Flujo 2), `DASHBOARD_BOT_TOKEN_VINCULO` en `.env`, importar y activar `workflows/Flujo 3 — Telegram Vínculo de Cuenta.json` con su credencial, y una **URL pública HTTPS** para el webhook (`WEBHOOK_URL`, p. ej. ngrok).
3. **Credenciales de n8n** para que el Flujo 1 real funcione: Postgres (`postgres:5432`, BD `ecommerce_tesis`, usuario `n8n_user`) y SMTP (`mailpit:1025`, sin auth ni TLS), asignadas a los nodos; o restaurar desde `backups/2026-09-16_21-36/` con `restore.ps1` (revisar antes qué pisa). Recién ahí se puede correr `demo_en_vivo.ps1` de verdad.
4. **Revisión visual con sesión real** (cuenta demo o propia) de las pantallas, con la fuente Inter cargada desde Google Fonts.
5. **`.env.example`**: el permiso no me dejó editarlos. Agregar en el de la raíz (y, si querés, en `dashboard-api/.env.example`): una nota sobre `DASHBOARD_FRONTEND_URL` (por defecto `http://localhost:8080`; con Vite `http://localhost:5173`; tiene que estar en `CORS_ORIGINS`; `DASHBOARD_GOOGLE_REDIRECT_URI` es el callback del backend y debe coincidir con Google Cloud Console), cambiar el valor de ejemplo de `DASHBOARD_FRONTEND_URL` a `http://localhost:8080`, y agregar las variables nuevas con su valor por defecto: `DASHBOARD_TRUSTED_PROXIES=dashboard-web`, `DASHBOARD_ENABLE_DOCS=false`, `DASHBOARD_ALLOW_REGISTRATION=true`. Además `DASHBOARD_DATABASE_URL` no la lee nadie (la API usa `DATABASE_URL`).
6. Verificar en tu `.env` que `DASHBOARD_JWT_SECRET`, `DASHBOARD_N8N_SECRET` y `DASHBOARD_ENC_KEY` son valores propios y que `DASHBOARD_N8N_SECRET` es el mismo que usa n8n (el compose ya se lo pasa a ambos).

## Próximo paso
Cargar en la UI de n8n las credenciales Postgres y SMTP para cerrar el punto 9 de C4 y poder correr `demo_en_vivo.ps1` de verdad (el usuario). Revisar a ojo las pantallas con la sesión real (ver lista en el reporte de C5). C6 (QA de seguridad) está cerrada; falta C7 (push a una rama que no sea `main`) y los pendientes del usuario de arriba.
